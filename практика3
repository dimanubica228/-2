fun main() {
    val m = 10 // Замените на желаемое целое число m
    val n = 3 // Замените на желаемое целое число n

    if (m % n == 0) {
        println("Частное от деления: ${m / n}")
    } else {
        println("$m на $n нацело не делится")
    }
}
